export function downloadText(name:string,text:string,type='text/plain'){const b=new Blob([text],{type});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=name;a.click();URL.revokeObjectURL(u)}
export function rowsToCsv(rows:Record<string,unknown>[]){if(!rows.length)return'';const keys=Object.keys(rows[0]);const esc=(v:unknown)=>`"${String(v??'').replaceAll('"','""')}"`;return [keys.map(esc).join(','),...rows.map(r=>keys.map(k=>esc(r[k])).join(','))].join('\n')}
export function downloadSvg(containerId:string,name:string){const svg=document.querySelector(`#${containerId} svg`);if(!svg)return;const xml=new XMLSerializer().serializeToString(svg);downloadText(`${name}.svg`,xml,'image/svg+xml')}
export async function downloadPng(containerId:string,name:string){const svg=document.querySelector(`#${containerId} svg`) as SVGElement|null;if(!svg)return;const xml=new XMLSerializer().serializeToString(svg);const blob=new Blob([xml],{type:'image/svg+xml'});const url=URL.createObjectURL(blob);const img=new Image();img.onload=()=>{const c=document.createElement('canvas');c.width=Math.max(1200,svg.clientWidth*2);c.height=Math.max(600,svg.clientHeight*2);const x=c.getContext('2d');if(x){x.fillStyle=getComputedStyle(document.body).backgroundColor;x.fillRect(0,0,c.width,c.height);x.drawImage(img,0,0,c.width,c.height);c.toBlob(p=>{if(p){const u=URL.createObjectURL(p);const a=document.createElement('a');a.href=u;a.download=`${name}.png`;a.click();URL.revokeObjectURL(u)}},'image/png')}URL.revokeObjectURL(url)};img.src=url}
